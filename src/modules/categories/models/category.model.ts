export interface CategoryModel {
  bgImageUrl: string;
  title: string;
  description: string;
  infoPoints?: string[];
  link: string;
  level: string;
  totalCourses: number;
  totalDuration: number;
}
